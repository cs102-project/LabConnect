package me.labconnect.webapp.controller;

public class LiveSessionController {
    
    /*
    
    Note: would it be *easier* to use websockets here? something to quickly check out perhaps, just in case.
    
    /api/livesession/
    GET -> get live sessions
    POST -> start livesession
    
    /api/livesession/{id}
    GET -> details of live session
    
    (to be expanded...)
    
    */
    
}
